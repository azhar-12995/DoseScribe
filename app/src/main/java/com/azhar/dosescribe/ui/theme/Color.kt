package com.azhar.dosescribe.ui.theme

import androidx.compose.ui.graphics.Color

// ── Primary brand ─────────────────────────────────────────────
val BrandBlue = Color(0xFF0982BA)          // primary accent
val BrandBlueDark = Color(0xFF066992)      // pressed / dark variant
val BrandBlueLight = Color(0xFFE8F4FA)     // soft tinted container
val BrandBlueSoft = Color(0xFFF2F9FC)      // very light wash for banners

// ── Semantic ──────────────────────────────────────────────────
val SuccessGreen = Color(0xFF2E7D32)
val SuccessGreenLight = Color(0xFFE8F5E9)
val ErrorRed = Color(0xFFC62828)
val ErrorRedLight = Color(0xFFFFEBEE)
val WarningOrange = Color(0xFFF0A030)
val WarningOrangeLight = Color(0xFFFFF4E0)

// ── Accent / category cards ───────────────────────────────────
val CardGreen = Color(0xFF2E9E6E)
val CardPurple = Color(0xFF7B5EBF)
val CardOrange = Color(0xFFF0A030)

// ── Neutrals (soft whites / off-whites) ───────────────────────
val SurfaceBg = Color(0xFFF6F8FB)          // app background — soft white
val SurfaceCard = Color(0xFFFFFFFF)        // card surface
val SurfaceElevated = Color(0xFFFAFBFD)    // elevated surface
val DividerSoft = Color(0xFFE6EAF0)
val TextPrimary = Color(0xFF1A2233)        // near-black, calmer than pure black
val TextSecondary = Color(0xFF5B6675)
val TextMuted = Color(0xFF8A94A6)

// ── Legacy ────────────────────────────────────────────────────
val Purple200 = Color(0xFFBB86FC)
val Purple500 = Color(0xFF6200EE)
val Purple700 = Color(0xFF3700B3)
val Teal200 = Color(0xFF03DAC5)
